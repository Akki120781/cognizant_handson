# Merge Resolution Notes

The conflict in `hello.xml` was resolved by reviewing both branch versions and replacing the conflict markers with a single final message. Backup files generated by merge tools are covered by the `*.orig` ignore rule.
