# Exercise 1: Singleton Pattern

This exercise implements a `Logger` utility class with:

- a private static instance
- a private constructor
- a public `getInstance()` method

`SingletonPatternDemo` verifies that both references point to the same object.

