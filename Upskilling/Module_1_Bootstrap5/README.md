# Module 1: Bootstrap 5

Open `index.html` in a browser to review Bootstrap 5 grid, forms, buttons, navbars, cards, spacing, colors, display utilities, borders, positioning, icons, modal, accordion, and Sass customization notes.

The `scss` folder contains the customization source for primary color and border radius.
